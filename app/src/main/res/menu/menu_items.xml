<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:title="Terms &amp; Conditions"
        android:id="@+id/termsConditions"
        app:showAsAction="never" />

    <item
        android:menuCategory="secondary"
        android:id="@+id/settings"
        android:title="Settings"
        android:icon="@drawable/baseline_settings_24"
        app:showAsAction="never"/>
</menu>

<!--order to connect Settings Fragment to NavGraph and its optionMenu,
    Fragment id and menuItem id should be same -->

<!--here android:menuCategory="secondary" is used because
     everytime when back button is pressed it redirects back only to
     HomeFragment, in order to resolve it menuCategory="secondary"
     attribute is used to redirects back to previous fragment instead of
     HomeFragment(Default one)-->